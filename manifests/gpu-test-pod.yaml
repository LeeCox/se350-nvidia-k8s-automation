apiVersion: v1
kind: Pod
metadata:
  name: ${POD_NAME}
  labels:
    purpose: gpu-validation
spec:
  restartPolicy: Never
  containers:
    - name: cuda
      image: nvidia/cuda:12.4.1-base-ubuntu24.04
      command: ["nvidia-smi"]
      resources:
        limits:
          nvidia.com/gpu: 1
